export const APP_NAME = 'Level Music API';
export const APP_DESCRIPTION = 'The Level Music API description';
export const API_VERSION = 'v1';

export const API_PREFIX = 'api/v1';

export enum Roles {
  ADMIN = 'Administrador',
  CLIENTE = 'Cliente'
}

export enum Estado {
  ACTIVO = 'Activo',
  INACTIVO = 'Inactivo'
}

export enum DocType {
  DNI = 'Dni',
  RUC = 'Ruc',
}